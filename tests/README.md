## Running the Tests

Activate the Python environment where the driver is installed, then install `pytest`:

```
pip install pytest
```

Next, change to the project's root directory (the directory containing the `tests/` folder), 
connect a RAVA8 device, and run:

```
pytest -v
```